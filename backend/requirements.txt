# 재현성을 위해 버전 고정 (2026-07-04, venv `archive` 기준)
fastapi==0.139.0
uvicorn[standard]==0.50.0
pydantic==2.13.4
pydantic-settings==2.14.2
pyjwt[crypto]==2.13.0
httpx==0.28.1

# LLM (M6 문구추천 — Claude Haiku 4.5)
anthropic==0.116.0

# rate limit (M5 — 공개/비용 엔드포인트 남용 방지, per-IP)
slowapi==0.1.10

# 테스트
pytest==9.1.1
